# Отправляем куки, делаем авторизацию

```javascript
Router.any('/', class extends Controller {
    constructor(req, res) {
        super(req, res);
        
        
        if (['GET', 'POST'].includes(req.method)) {
            
        }
    }
});
```